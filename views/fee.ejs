<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Fee Payment - <%= user.name %></title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .info-section {
      border: 1px solid #ccc;
      padding: 15px;
      margin: 15px;
      border-radius: 5px;
      background-color: #f9f9f9;
    }
    .pay-btn {
      padding: 10px 20px;
      background-color: #007bff;
      color: #fff;
      border: none;
      border-radius: 4px;
      cursor: pointer;
      margin-top: 15px;
    }
    .pay-btn:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <header>
    <h1>Fee Payment</h1>
    <nav>
      <a href="/dashboard?user_id=<%= user.user_id %>">Dashboard</a>
      <a href="/logout">Logout</a>
    </nav>
  </header>
  
  <main>
    <div class="info-section">
      <h2>User Details</h2>
      <p><strong>Name:</strong> <%= user.name %></p>
      <p><strong>Roll Number:</strong> <%= user.roll_number %></p>
      <p><strong>Email:</strong> <%= user.email %></p>
    </div>
    
    <div class="info-section">
      <h2>Semester Details</h2>
      <% if (semester) { %>
        <p><strong>Semester Number:</strong> <%= semester.semester_no %></p>
        <p><strong>Start Date:</strong> <%= semester.start_date %></p>
        <p><strong>End Date:</strong> <%= semester.end_date %></p>
        <p><strong>Fee (ETH):</strong> <%= semester.fees %></p>
      <% } else { %>
        <p>No active semester found. Please check back later.</p>
      <% } %>
    </div>
    
    <% if (semester) { %>
      <div style="width: 100%; text-align: left; padding-left: 20px;">
        <form id="feeForm" action="/api/fees" method="POST" style="display: inline-block;">
          <input type="hidden" name="user_id" value="<%= user.user_id %>">
          <input type="hidden" name="amount" value="<%= semester.fees %>">
          <button type="submit" class="pay-btn">Pay Fee</button>
        </form>
      </div>
    <% } %>
  </main>
  
  <footer>
    <p>&copy; 2025 College XXX</p>
  </footer>
</body>
</html>
