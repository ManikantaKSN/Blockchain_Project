<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Dashboard - <%= title %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header>
    <h1>Dashboard</h1>
    <nav>
      <a href="/course">Course Registration</a> |
      <a href="/certificate?user_id=<%= user_id %>">Certificate Issuance</a> |
      <a href="/fees?user_id=<%= user_id %>">Fee Payment</a> |
      <a href="/logout">Logout</a>
    </nav>
  </header>
  <main>
    <h2>Your Registered Courses</h2>
    <% if (courses.length > 0) { %>
      <ul>
        <% courses.forEach(course => { %>
          <li><%= course.course_name %> (Course ID: <%= course.course_id %>)</li>
        <% }); %>
      </ul>
    <% } else { %>
      <p>You have not registered for any courses yet.</p>
    <% } %>
  </main>
  <footer>
    <p>&copy; 2025 College Name</p>
  </footer>
</body>
</html>
